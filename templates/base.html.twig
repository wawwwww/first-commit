<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('css/main.css') }}">
        {% block stylesheets %}{% endblock %}
    </head>

    <h1 class="babar"><img src="{{ asset('telechargement.png') }}" alt="logo-persone-agée" height="50px width="50px"><strong>Besoins Du Quotidien</strong></h1>
    <body>

    <nav class="navbar navbar-expand-lg navbar-light bg-danger">

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('accueil') }}">Accueil <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('assos') }}">Qui somme-nous <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('service') }}">Services</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                </li>
            </ul>
            <div class="d-flex ml-auto" >
                <a class="nav-link " href="{{ path('fos_user_registration_register') }}">Inscription</a>
                <a class="nav-link " href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
            </div>
        </div>
    </nav>
        {% block body %}{% endblock %}
        {% block javascripts %}<script src="https://ajax.aspnetcdn.com/ajax/jquery/jquery-1.9.0.js">

        $(function(){
            $(".bonjour").hide();
            $("#start").click(function(){
                $('div').animate({left: '600px'}, 3000);
                $('div').animate({width: '200px'}, 3000);
                $('div').animate({height: '600px'}, 3000);
                $('div').animate({left: '800px'}, 3000);
                $('div').animate({opacit: '0.2'}, 3000);
                $('div').animate({left: '0px'}, 3000);
                $('div').animate({width: '100'}, 3000);
                $('div').animate({width: '100'}, 3000);
                $('div').animate({opacity: "0.2"}, 3000);
                $('div').animate({opacity: '1'}, 3000);
                $('div').animate({opacity: '0'}, 3000);
                $("dive").hide();





            });
            $("#stop").click(function(){
                $("div").stop(true);


            });
        });



        </script>{% endblock %}
          </div>
    </body>
</html>
